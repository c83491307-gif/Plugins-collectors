export class SmartSendTime{constructor(){this.stats=new Map()}record(hour,{delivered=0,read=0,replied=0}={}){const s=this.stats.get(hour)||{delivered:0,read:0,replied:0};Object.assign(s,{delivered:s.delivered+delivered,read:s.read+read,replied:s.replied+replied});this.stats.set(hour,s)}best(){return [...this.stats.entries()].sort((a,b)=>this.rate(b[1])-this.rate(a[1]))[0]?.[0]??9}rate(s){return s.delivered?((s.read+s.replied)/s.delivered):0}}