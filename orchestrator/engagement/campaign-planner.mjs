export class CampaignPlanner{constructor({templates=null}={}){this.templates=templates;this.campaigns=new Map()}create(c){const x={status:"draft",nodes:[],...c};this.campaigns.set(x.id,x);return x}clone(id,newId){const x=this.campaigns.get(id);if(!x)throw Error("campaign not found");return this.create({...structuredClone(x),id:newId,status:"draft"})}approve(id){const x=this.campaigns.get(id);if(!x)throw Error("campaign not found");x.status="approved";return x}schedule(id,at){const x=this.campaigns.get(id);if(!x||x.status!=="approved")throw Error("campaign must be approved");x.scheduledAt=at;x.status="scheduled";return x}validateTemplate(name,vars=[]){if(!this.templates)return false;const t=this.templates.get(name);return !!t&&t.approved&&vars.every(v=>t.body.includes("{{"+v+"}}"))}graph(id,nodes){const x=this.campaigns.get(id);if(!x)throw Error("campaign not found");x.nodes=nodes;return x}get(id){return this.campaigns.get(id)}}