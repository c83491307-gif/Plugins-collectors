export class RevenueAttribution{constructor(){this.events=[]}track(e){this.events.push({...e,at:e.at??Date.now()});return e}campaign(campaignId){const a=this.events.filter(e=>e.campaignId===campaignId);return {events:a.length,revenue:a.reduce((s,e)=>s+Number(e.revenue||0),0),orders:a.filter(e=>e.type==="order").length}}cohort(field="cohort"){const out={};for(const e of this.events){const k=e[field]??"unknown";out[k]??={events:0,revenue:0};out[k].events++;out[k].revenue+=Number(e.revenue||0)}return out}}