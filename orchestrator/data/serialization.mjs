export function toJSON(rows){return JSON.stringify(rows,null,2)}export function fromJSON(text){return JSON.parse(text)}export function toCSV(rows){if(!rows.length)return"";const keys=[...new Set(rows.flatMap(r=>Object.keys(r)))];const esc=v=>JSON.stringify(v??"");return[keys.join(","),...rows.map(r=>keys.map(k=>esc(r[k])).join(","))].join("\n")}export function fromCSV(text){const [head,...lines]=text.trim().split(/\r?\n/);if(!head)return[];const keys=head.split(",");return lines.map(l=>{const vals=l.split(",");return Object.fromEntries(keys.map((k,i)=>[k,vals[i]?.replace(/^"|"$/g,"")??""]))})}