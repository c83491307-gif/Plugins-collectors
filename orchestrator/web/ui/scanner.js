window.CreazzyScanner={events:[],last:null,ingest:function(event){const normalized={timestamp:new Date().toISOString(),type:event.type||"scan",mode:event.mode||"barcode",value:String(event.value||""),source:event.source||"android"};this.last=normalized;this.events.push(normalized);this.events=this.events.slice(-100);window.dispatchEvent(new CustomEvent("creazzy-scanner",{detail:normalized}));return normalized;},send:async function(event){const normalized=this.ingest(event);try{await fetch("/api/scanner/events",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(normalized)});}catch(_){/* local-first; backend may be offline */}return normalized;}};window.addEventListener("creazzy-barcode",e=>window.CreazzyScanner.send({mode:"barcode",value:e.data,type:"barcode"}));